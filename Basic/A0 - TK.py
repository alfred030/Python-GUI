from tkinter import *   # this all of the function and variables from tkinter.

root = Tk()  # this is to create the top level and assign it to the  variable call root an it will have the name tk.
Label(root, text="Hello, Tkinter!").pack() #.pack is to display the label with hello tkinter.
root.mainloop()  # is to display the main root window with all the information.
